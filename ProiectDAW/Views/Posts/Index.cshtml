@if (ViewBag.Msg != null)
{
    <h2 class="alert-success p-3 rounded-3 text-center mb-5">@ViewBag.Msg</h2>
}

@foreach (var post in ViewBag.Posts)
{
    <partial name="PostIndexShow" model="post" />
}

<a href="/Posts/New">Adaugare postare</a>

